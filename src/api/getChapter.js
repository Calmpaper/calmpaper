import client from './client'
import { getChapterByBookQuery } from './chapters/queries'

export const getChapter = async () => {
  return {}
  // const {
  //   data: { chapter },
  // } = await client.query(getChapterByBookQuery).toPromise()

  // return chapter
}
